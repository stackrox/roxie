# PR Review: #77

**Commit:** `55eb5fe3f58d474bd6290fbe429430628ac66941`
**Reviewed:** 2026-03-27

---

🔴 **2 critical issues** | ⚠️ **1 important issue**

---

## 🔴 Critical Issues (P0)

### Feature flags not applied to Helm deployments

**File:** `internal/deployer/deploy_via_helm.go:262-264`

**Action:** Apply feature flag overrides to Helm values using `mergeWithEnvVarSupport` in both `createCentralValues` and `createSecuredClusterValues` methods, similar to the operator CR methods.

**Risk:** Feature flags silently ignored when deploying via Helm, breaking the documented CLI behavior. Users may deploy with incorrect configuration without warning.

---

### Non-deterministic ordering in mergeEnvVars output

**File:** `internal/deployer/feature_flags.go:135-140`

**Action:** Sort the envVars slice by name before returning to ensure deterministic output. Go map iteration is random, causing different envVar orderings across runs.

**Risk:** Non-deterministic YAML output causes unnecessary CR updates/diffs when the same flags are applied repeatedly, potentially triggering pod restarts and flaky tests.

---

## ⚠️ Important Issues (P1)

### Missing integration test for SetFeatureFlags

**File:** `internal/deployer/deployer.go:445-471`

**Action:** Add test coverage for `SetFeatureFlags` method to verify error handling, logging behavior, and interaction with the Deployer struct.

**Impact:** The public API method `SetFeatureFlags` has no direct test coverage. Only the internal helper functions are tested, leaving gaps in validation logic and error paths.
